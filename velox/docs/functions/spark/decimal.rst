=================
Decimal functions
=================

.. spark:function:: unscaled_value(x) -> bigint

    Return the unscaled bigint value of a short decimal ``x``, assuming it fits in a bigint.
    Supported types are: SHORT_DECIMAL.
